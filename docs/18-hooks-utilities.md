# 18 HOOKS & UTILITIES

Currently, state is managed locally within components. As the project grows, standard logic (like API calling) can be extracted into custom React hooks (e.g., `useJobs.js`, `useGenerateEmail.js`).
