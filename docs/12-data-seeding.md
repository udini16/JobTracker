# 12 DATA SEEDING

If the live web scraper (Puppeteer) fails due to anti-bot protections, the `scraper.js` file contains fallback mock data.

The mock data seeds the system with 2 placeholder jobs so that development and testing of the LLM and Email pipelines can continue uninterrupted.
