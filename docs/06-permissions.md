# 06 PERMISSIONS

## Role-Based Access Control (RBAC)

Currently, this application is designed as a single-user personal tool.
There are no distinct user roles (e.g., Admin, User). 

Future implementations should include user authentication (JWT) to ensure users can only access their own jobs and resumes.
