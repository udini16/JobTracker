# 20 ACCESSIBILITY

- Standard HTML5 semantic elements are used.
- Inputs have associated labels.
- Tailwind focus rings (`focus:ring-2`) are implemented to ensure the application is navigable via keyboard.
